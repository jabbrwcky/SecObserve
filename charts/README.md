# Helm charts

The Helm charts in the subfolders are published as OCI containers to `ghcr.io/SecObserve/charts/secobserve:1.0.13`.

Charts can be installed using `helm install secobserve --version 1.0.13 oci://ghcr.io/SecObserve/charts/secobserve`
